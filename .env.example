# 实时大模型连接测试所需的环境变量示例。
# 复制本文件为 .env 并填入真实值后，即可通过 `make test-live` 运行集成测试。
# （不配置时 `make test` 中的实时用例会自动跳过，不影响 CI。）

# 大模型 API Key
TEST_OPENAI_API_KEY=sk-tp-hZ741cHeSlrlI2M02H74h2U3xlxQgJKMNQAb4JdpWDZiy2LJ

# 网关 / Proxy 基础地址（如 https://api.openai.com/v1 或自建代理）
TEST_OPENAI_BASE_URL=https://api.lkeap.cloud.tencent.com/plan/v3

# 模型名（默认 gpt-4o-mini，可改为 deepseek-reasoner 等支持推理内容的模型）
TEST_OPENAI_MODEL=hy3
